# Virtual environments
ragollamaenv/
venv/
env/
.env

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
build/
dist/
*.egg-info/

# Jupyter Notebook checkpoints
.ipynb_checkpoints

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
.coverage
.tox/
.nox/
coverage.xml
htmlcov/

# Translations
*.mo

# Django stuff:
*.log
local_settings.py
db.sqlite3

# Flask stuff:
instance/
*.db

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# macOS
.DS_Store

# VS Code
.vscode/

# JetBrains
.idea/

# Other
*.bak
*.tmp

# Keep node_modules out if present
node_modules/
ragollamaenv/# show whether the folder is currently tracked
git ls-files --error-unmatch ragollamaenv 2>/dev/null && echo "tracked" || echo "not tracked"

# If it is tracked, untrack it and commit
git rm -r --cached ragollamaenv
git commit -m "Remove ragollamaenv from repo and add to .gitignore"
git push
zsh: command not found: #
cat: .gitignore: No such file or directory
zsh: command not found: #
not tracked
zsh: command not found: #
fatal: pathspec 'ragollamaenv' did not match any files
On branch main
Your branch is up to date with 'origin/main'.

nothing to commit, working tree clean
remote: Invalid username or token. Password authentication is not supported for Git operations.
fatal: Authentication failed for 'https://github.com/rcalderonmadrid/RAG_repo.git/'